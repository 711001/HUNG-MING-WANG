#include <stdio.h>
int main(){
    int a,b,c;
    for(a=1;a<5;a++){//以下是三重迴圈
        for(b=1;b<5;b++){
            for(c=1;c<5;c++){
                if(a!=b&&a!=c&&b!=c){//確保三數字不重複
                    printf("%d,%d,%d\n",a,b,c);
                }
            }
        }
    }
    return 0;
}
